Usage
=====

This section is intentionally slim for now. Use the reference pages for details.

- Install: see :doc:`installation`.
- Explore commands quickly with ``python examples/demo.py --help`` (generate a sample repo, list commits, try rewrites).
- Reference:
  - :doc:`repository` for opening repos, HEAD, and commit listing.
  - :doc:`revision_parsing` for resolving git rev specs.
  - :doc:`history_rewrite` for amend/rewrite/squash/rebase/filter/remove-path flows.
  - :doc:`backups` for creating backup refs.
